<h1>9095 1, 2, 3 더하기</h1>
<h2>문제</h2>
정수 4를 1, 2, 3의 합으로 나타내는 방법은 총 7가지가 있다. 합을 나타낼 때는 수를 1개 이상 사용해야 한다.

1+1+1+1<br>
1+1+2<br>
1+2+1<br>
2+1+1<br>
2+2<br>
1+3<br>
3+1<br>
정수 n이 주어졌을 때, n을 1, 2, 3의 합으로 나타내는 방법의 수를 구하는 프로그램을 작성하시오.

<h2>입력</h2>
첫째 줄에 테스트 케이스의 개수 T가 주어진다. 각 테스트 케이스는 한 줄로 이루어져 있고, 정수 n이 주어진다. n은 양수이며 11보다 작다.

<h2>풀이 방법</h2>
예를 들어 5를 만들기 위해서는
4 + 1, 3 + 2, 2 + 3 가 가능하다
이렇게 하면 4를 만드는 경우의 수 + 3을 만드는 경우의 수 + 2를 만드는 경우의 수가 5를 만드는 경우의 수가 된다
이를 이용하면 1, 2, 3을 제외한 모든 수는 -1, -2, -3을 한 수를 만드는 경우의 수의 합이기 때문에 dp를 이용해서 풀이 가능하다
<br>
dp[n] =dp[n-1]+dp[n-2]+dp[n-3] (n>3) 이 된다

<h2> 코드 </h2>

```cpp
#include <iostream>
using namespace std;

int dp[12] = {0};
int main()
{
    dp[1] = 1;
    dp[2] = 2;
    dp[3] = 4;
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    for(int i=4;i<=11;i++)
    {
        dp[i]+= dp[i-1]+dp[i-2]+dp[i-3];
    }
    int n;
    cin >> n;
    while(n--)
    {
        int a;
        cin >> a;
        cout << dp[a]<<"\n";
    }

    return 0;
}
```
